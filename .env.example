# Public market-data collection does not require credentials for the Phase 00 scope.
# Leave private keys out of this repository. If a later phase needs authenticated
# market data, store values only in a local .env file that is ignored by git.

CROSS_VENUE_ENV=development

